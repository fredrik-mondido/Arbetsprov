import React from 'react';
import ActiveTodo from '../components/active-todo';

const ActiveTodoView = (props) => {

    return (
        <ActiveTodo {...props}/>
    )
}

export default ActiveTodoView;